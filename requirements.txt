fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3

# Detection. CPU-only torch wheels (much smaller, no CUDA) so the deploy
# image stays well within PaaS build/image size limits.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
ultralytics
opencv-python-headless

# YOLO-World's open-vocabulary text encoder (CLIP). Ultralytics only
# auto-installs this on first use if missing, which needs git + network
# access at runtime — pin it here so it's baked into the build instead.
git+https://github.com/ultralytics/CLIP.git
